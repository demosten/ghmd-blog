<!-- CUSTOM HEADER: This overrides ghmd/templates/partials/header.html.jinja -->
<!-- This is an example showing how to customize your blog's header -->
<header class="site-header">
    <button class="theme-toggle" id="theme-toggle" aria-label="Toggle theme">
        <span id="theme-icon">🌙</span>
    </button>
    <div class="header-content">
        <a href="{{ config.get_asset_url('index.html') }}" class="site-title">{{ config.title }}</a>
        {% if config.description %}
        <p class="site-description">{{ config.description }}</p>
        {% endif %}

        <!-- Example: Add navigation links -->
        <nav class="header-nav" style="margin-top: 1rem;">
            <a href="{{ config.get_asset_url('index.html') }}" style="margin-right: 1rem;">Home</a>
            <a href="{{ config.get_asset_url('about.html') }}" style="margin-right: 1rem;">About</a>
            <a href="https://github.com/demosten/ghmd-blog" target="_blank" rel="noopener">GitHub</a>
        </nav>
    </div>
</header>
